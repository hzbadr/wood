%fieldset
  .form-group
    = f.label :product_id, 'Product'
    = f.select :product_id, options_from_collection_for_select(Product.all, :id, :name, f.object.product_id), {include_blank: true}, {class: 'form-control'}
  .form-group
    = f.label :quantity
    = f.text_field :quantity, class: 'form-control'
  .form-group
    = f.hidden_field :_destroy
    = link_to "Remove", '#', class: "remove_fields"